# Strict Wikipedia → Wikidata bridge — ARG_LPF 2024

Status: measurement in progress on the isolated lab branch.

This checkpoint exists to trigger and then record the strict bridge workflow added in the preceding commit. The accepted result must use the retained historical-squad parser, only leading player-article links, and an explicit Wikidata `P31 = human` gate. Provisional figures from earlier, looser bridge experiments must not be promoted as final identity coverage.
