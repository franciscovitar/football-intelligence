@AGENTS.md
@WORKFLOW.md
@AI/PROMPT_ROUTER.md

# Claude Code

Treat `AGENTS.md` and `WORKFLOW.md` as the canonical repository instructions.

Use `AI/PROMPT_ROUTER.md` to select task-specific procedures when useful.

Do not load the entire `AI/prompts/` library by default. Read only the prompt files selected for the current task.

Do not duplicate canonical rules here.

Add Claude-specific instructions only when they cannot reasonably belong in the canonical files or router.
